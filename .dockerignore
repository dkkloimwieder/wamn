/target
/components/target
/components/no-std/target
.git
.dolt
.beads
docs
# deploy/ contains `include_str!` build inputs (including sql/run-state.sql),
# so it must remain in the build context.
vendor
