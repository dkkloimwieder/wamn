This interim stage 3 report retains line counts measured at `48ebff5bed99f45c0b3685323ced4e5b837c412a`. Each completed run below names its own source commit. Stage 3 remains incomplete. The latest retained workspace run failed. WMS startup and Receiving postcommit passed. Receiving's latest default run failed during operator recovery.

Application scenarios now live in [Receiving tests](../../../../apps/wamn_receiving/tests/) and [WMS tests](../../../../apps/wamn_wms/tests/). Their native test crates own setup, application assertions, measurements, and results. Receiving's Rust cluster caller landed in `02b40a9d`, its recovery caller in `9bda6080`, and its thin lifecycle script in `77ebc72a`. WMS's Rust cluster caller landed in `4c155183`. [Receiving's connected ordinary run](receiving-compile-003/README.md) passed 18 cases and ignored 12 cluster cases at `9bda6080`. [WMS's ordinary run](wms-runner-004/README.md) passed 16 cases and ignored eight live cases at `cd1b24b5`.

The line counts below include comments and blank lines at this report's source. The three cluster scripts contain kind, Helm, and Docker operations. Rust owns their setup decisions, readiness checks, application assertions, and result capture. The pilot scripts dispatch to the native test command and retain their explicit Docker lifecycle operations.

| Entrypoint | Lines |
| --- | ---: |
| [Receiving lifecycle](../../../../tools/receiving-cluster-journey-run) | 86 |
| [WMS lifecycle](../../../../tools/wms-cluster-journey-run) | 88 |
| [RC lifecycle](../../../../tools/rc-gate-run) | 61 |
| [Pilot execution](../../../../tools/agent-pilot-run) | 22 |
| [Pilot grading](../../../../tools/agent-pilot-grade) | 8 |

[Shared environment functions](../../../../test-support/harness/src/environment.rs) call existing control-library provisioning, publication, data-access reconciliation, and CDC functions. The existing [JourneyDocument](../../../../test-support/harness/src/journey.rs) remains the shared input owner. [Infrastructure modules](../../../../test-support/infrastructure/lib.rs) own typed YAML rendering, private credentials, platform installation, native workload observations, telemetry, and executor lifecycle assertions. Application assertions stay in the application test crates. The [first rendering run](rendering-001/report.md) passed 25 tests at `1e87eb4d`. The [retained case comparison](rendering-port-001/report.md) preserves 183 shell assertions and their Rust destinations, including cases specific to retired shell arguments.

The [platform extraction run](platform-rendering-001/report.md) passed six cases at `3b858fbe`. The [first app compile attempts](app-owners-001/report.md) and [second attempt](app-owners-002/report.md) remain recorded. The [corrected run](app-owners-003/report.md) passed both selected commands at `3406e858`, with every ignored case listed separately. The [retained rendering run](retained-rendering-tests-001/README.md) preserves its generated-output test failure. Its [focused correction](retained-rendering-tests-002/README.md) passed at `c8914883` without changing the production script bytes.

Private test modules moved out of [run-plane source](../../../../crates/schema/control/src/run_plane/tests.rs), [provisioning source](../../../../services/ctl/src/provision_project_env/tests.rs), and [PostgreSQL claims source](../../../../crates/platform/runtime/src/plugins/wamn_postgres/claims/tests.rs). These extractions landed in `1067be26`, `1cf7d5df`, and `e25c1cd8`. Their [test run](private-tests-001/README.md) reports 75 run-plane passes and 37 provisioning passes. Claims reports 39 passes and one failure, but nine reported passes did not execute their optional subjects. The [complete classification](private-tests-001/classified-results.json) names those nine cases and the retained missing-database failure.

Receiving generation tests moved to the app in `f5ad982d`. The [generation run](app-generation-tests-001/README.md) passed all 33 Receiving and 27 generator tests with no ignored cases. Its [function comparison](app-generation-tests-001/body-comparison.json) and [source correspondence](app-generation-tests-001/source-correspondence.json) preserve the exact extraction and single function-body path change. Acme gained its [native SQLx test owner](../../../../apps/client_acme_receiving/tests/Cargo.toml) in `a2e0b2fb`. Commit `b8f96067` moved 27 unchanged SQLx metadata files to their app test owners. Both [offline SQLx targets](app-sqlx-tests-001/report.md) passed at that source, without running PostgreSQL or guest code.

Commit `5a8dea68` placed Receiving's descriptor checks, HTTP fixture, and terminal scenario in its [app test owner](../../../../apps/wamn_receiving/tests/operator_pty.py). Shared [terminal mechanics](../../../../crates/client/terminal/tests/operator_pty.py) and [live support](../../../../crates/client/terminal/tests/live_support.py) remain independent of Receiving. WMS uses those shared helpers directly. The [source checks](pty-ownership-001/source-checks.json) preserve exact descriptor and scenario assertions. Host and token inputs now enter each shared session explicitly.

The [Receiving terminal run](pty-receiving-001/report.md) passed at `47391bbc` after 4.860752707 seconds. It exercised startup, pending barriers, target replacement, SIGTERM, SIGINT, Ctrl-C, HTTP bytes, credential assertions, and terminal restoration. It ran one existing driver against a local HTTP fixture, with no skipped arm. All 1,884 captured source files and the binary remained unchanged. It did not change a field, run platform services, or execute the separate live caller's `check_descriptors` assertion.

The [local WMS terminal run](pty-wms-001/README.md) passed both response modes at `47391bbc` after 4.508828254 seconds. Each mode sent one read and one move, and spent controls sent nothing. The partial mode displayed the committed result and failure, and both modes restored the terminal. The actual binary used synthetic HTTP and database responses. This run establishes no SQL commit, object storage write, or platform execution. The [source and binary record](pty-wms-command-001/README.md) retains unchanged inputs and the missing exact binary build commit.

WMS's [released-route run](wms-cluster-live-001/README.md) passed at `04b72bd0`, including contention, replay, label storage, environment isolation, and the idle materializer. Its [terminal run](wms-terminal-live-001/README.md) passed successful and partial-completion requests. Its [browser demonstration](wms-browser-live-001/README.md) passed proxy reachability and cleanup, without browser interaction or successful-page assertions. Its [startup run](wms-startup-live-001/README.md) failed with ratio `23.809243876261803` against the retained limit of `12`. Cold and restarted requests completed, but the final cache comparison did not execute. The [combined WMS result](wms-extraction-001/README.md) retains that failure and successful source and resource cleanup checks for all four runs.

The [saved-trace review](wms-startup-review-001/review.json) replayed the retained calculation and also exceeded the limit. Native B removed `wamn.component.instantiate` in `6feb01ac`, so the recorded denominator contains SQL time alone. This identifies an obsolete measurement input, without establishing an extraction timing regression or the current performance cause. No equivalent native span was found. The owner approved removal of the obsolete ratio, and commit `709aacba` retains requests, traces, recovery limits, and cache comparisons. The earlier result remains failed.

The [corrected WMS startup run](wms-startup-live-002/README.md) passed at `fb6c2b2e`, with results recorded in `401ff97d`. Cold, restarted, and steady requests returned HTTP 200. Recovery took 75 seconds, below the retained 120-second limit. Both traces passed, and all four compiled cache entries retained their contents and metadata. The [controller record](wms-startup-cargo-002/README.md) confirms unchanged source and successful cleanup.

Receiving's [first full run](receiving-cluster-live-001/README.md) stopped on a refused NodePort connection after its earlier cases and CDC setup. The [second run](receiving-cluster-live-002/README.md) passed endpoint readiness and both HTTP mutations, then timed out before the paired causal-event condition. The [third run](receiving-cluster-live-003/README.md) captured repeated `cannot enter component instance` errors at that stopping point. Commit `be96beb7` changed the materializer to the native asynchronous service entrypoint. The [fourth run](receiving-cluster-live-004/README.md) completed materializer causation, inspection-row, durable-acknowledgement, no-advisory, and telemetry checks, then panicked during Rustls provider setup. That failed run has no top-level success result.

After the provider setup correction in `25bece68`, the [fifth run](receiving-cluster-live-005/README.md) again completed those materializer and telemetry checks. Startup then failed with `invalid peer certificate: UnknownIssuer`. Later environment and operator checks did not run. The owner approved a separate certificate from the same issuer, with `127.0.0.1` among its permitted addresses. Commit `fb6c2b2e` supplies that certificate and the existing client identities while retaining peer verification.

The [sixth default run](receiving-cluster-live-006/README.md) at `fb6c2b2e` failed after 574.364 seconds, with results recorded in `ee6e4dce`. Materializer, telemetry, startup protocol, trace, and [environment isolation checks](receiving-cluster-live-006/cross-environment-workload.json) passed. Operator recovery then failed with `startup refusal must identify the observed container start`. The [captured transition](receiving-cluster-live-006/operator-recovery/operator-transition-3-state.json) shows a waiting container with its start time in the last termination record. Later operator recovery and final default assertions did not complete. Source and cleanup checks passed.

Commit `c4c4c8b0` changes [recovery checks](../../../../apps/wamn_receiving/tests/route_authentication_live/cluster/operator_recovery.rs) to read the last termination's start time when the previous snapshot reports a waiting container. The container identifier must be nonempty and match. All other assertions for process identity, timing, exit cause, and timeout logs remain. Commit `ed16ee43` declares `-zap-log-level=info` through [operator values](../../../../deploy/infra/values-wamn.yaml). These source changes await the seventh Receiving run.

The [native C scoped run](../native-c-advisories/scoped-native-008/README.md) separately passed one broker case, including foreign runtime and materializer attachment refusals. These scoped broker observations remain separate from the Receiving application results below.

The [first Receiving postcommit pair](receiving-postcommit-cargo-001/README.md) failed during its baseline gates-image build, before cluster creation or application assertions. [Recovered Docker output](receiving-postcommit-image-001/README.md) shows Rust 1.97 rejecting the materializer's asynchronous component metadata. After `73d258fc`, the [Docker component build](materializer-docker-001/README.md) passed with Rust 1.98 for materializer and retained one Cargo invocation per guest. Its artifact hashes differ from the standalone build, so these results claim no equality between those build paths. The [second pair launch](receiving-postcommit-cargo-002/README.md) failed because its launcher pre-created the required new result directory. The [third pair](receiving-postcommit-live-003/README.md) passed at source `965cd418`, with one selected test completing after 2039.678 seconds.

Both [baseline](receiving-postcommit-live-003/baseline/verdict.json) and [additive](receiving-postcommit-live-003/additive/verdict.json) installations passed all five retained test groups. Each preserved the approved inspection after duplicate delivery, recorded three blocked handler attempts, and observed a native `MSG_TERMINATED` advisory with one broker delivery. Later valid events progressed within 90 seconds in both installations. The [pair comparison](receiving-postcommit-live-003/pair.json) retains 95 unchanged overlay files, distinct base schema hashes, and the exact breaking-candidate ownership refusal. Its [separate observation](receiving-postcommit-cargo-003/post-run-observation.json) records removed owned resources and unchanged, clean source. This targeted pair does not execute default startup, development-environment, or operator-recovery cases.

[RC setup and Jobs](../../../../tests/integration/src/rc.rs) moved into Rust in `49388df8`. Its [first run](rc-live-command-001/README.md) failed on an image digest assertion, and its [second run](rc-live-command-002/README.md) failed strict administrator URL validation. The [third run](rc-live-003/README.md) passed at `1c4a2676`, executing the retained socket and trace-context tests. Host image identity checks remain separate from the Jobs' exact image and nonempty native image ID assertions. The passing record retains `wamn-0h0g.15.153` as `post-merge; not claimed`. Its separate cleanup record passes, while the original result's `cleanup: null` remains unchanged.

[Pilot execution and grading](../../../../tests/integration/src/agent_pilot/) moved into Rust in `e107cd89`. [All 37 ordinary pilot tests](pilot-compile-001/README.md) passed at `cc729d9e`, and [15 preparation tests](pilot-preparation-tests-001/README.md) passed after recorded grading inputs were excluded. [Replay of historical inputs](pilot-recorded-replay-001/report.md) reproduced six original outcomes and all 66 step verdicts. Run 030 lacked its hidden grading inputs, and runs 031 and 032 required recovered original inputs. This replay ran no agent or service and did not reconstruct the original worktrees or establish current application behavior.

The [first local pilot setup](pilot-stub-live-001/report.md), run `901s`, failed before the stub launched. Its driver used `1925bea2`, while its prepared product worktree remained at `9d16e940`. Setup exited `10` after 869.838997643 seconds because `/postgres` failed the required `/wamn_system` database check. Cleanup exited `0` after 2.299268930 seconds. Later records show removed named services and private environment files, closed reserved ports, and unchanged source. The frozen cluster remained untouched, and the prepared worktree and build cache remained available.

Commit `8671f63c` created the required system database before standup. The [second local setup](pilot-stub-live-002/report.md), run `902s`, used that driver with the same prepared product source `9d16e940`. Setup passed the database check but exited `10` after 216.759827766 seconds because the identity executable was absent. Cleanup exited `0` after 2.594984684 seconds. Its recorded observations show no named services or open reserved ports, with unchanged source. Neither setup launched the stub, an external agent, grading, or the Dock application.

Commit `7adc577e` added `wamn-identity` to the existing native build and its executable and hash checks. It kept the guest build invocation unchanged. The [controller build](pilot-live-build-005/README.md) passed after 3.806992749 seconds at that source.

The [third local pilot run](pilot-stub-live-003/report.md), run `903s`, passed with driver `7adc577e` and prepared product source `9d16e940`. Setup, stub launch, and cleanup exited `0` after 226.097401081, 0.197137268, and 3.015236766 seconds, respectively. Named resource, reserved port, and source observations passed. This test exercised the local stub lifecycle. It did not execute an external agent, grading, or the Dock application.

The [completed workspace run](sweep-001/run-001/README.md) at `5ef02888` exited 101 after 533.769 seconds with unchanged source. Its [final classification](sweep-001/run-001/classification-002/report.md) lists all 104 failures and 84 explicit skips. It preserves [every changed failure](sweep-001/run-001/classification-002/changed-failures.json) and [17 moved identities](sweep-001/run-001/classification-002/moved-failure-identities.json), with comparisons against the starting baseline and earlier steps. The 2333 reported test passes include those skips, and six doctests passed. Later [focused corrections](sweep-corrections-001/README.md) passed the three newly failing ordinary assertions. Those focused results do not replace the failed workspace result or the unexecuted live cases.

The [second workspace run](sweep-001/run-002/classification-002/report.md) at `bead5288` exited 101 after 137.013 seconds, with 160 named failures. Its restricted operating-system sandbox denied local sockets in 59 added cases. The existing chart case also stopped at a denied DNS socket, before Kubernetes discovery. Those actual failures remain recorded separately from source regressions. All tracked source bytes, modes, and HEAD remained unchanged.

The [third workspace run](sweep-001/run-003/classification-002/report.md) used the host context at `60adfdd3` and exited 101 after 121.916 seconds. All 101 failures match the first run's retained identities and causes. The chart case again reaches the retained Kubernetes discovery refusal. The three corrected ordinary cases pass. Its 2338 reported passes include 84 explicit skips, and six doctests pass. The captured source again remained unchanged.

The [socket comparison](sweep-001/run-003/classification-002/socket-refusals-now-passed.json) records passing rows for all 59 newly sandbox-refused cases. All 34 [moved application cases](sweep-001/run-003/classification-002/app-test-moves.json) also report passing results under their new owners. The full classified reports preserve every failed or skipped case, with no unresolved parser entries or cause reviews. These comparisons establish no result for work that returned early because its live inputs were absent. The retained workspace result remains failed.

The [fourth workspace run](sweep-001/run-004/classification-002/report.md) at `48ebff5b` exited 101 after 139.969 seconds. All 101 failure identities and causes match run 003, with none added, changed, absent, or duplicated. It reported 2338 passes, including 84 explicit skips, plus six passing doctests and two filtered schema regeneration cases. All 29,046 captured tracked files retained their bytes and modes, and HEAD remained unchanged. All 34 mapped application cases passed, and 17 earlier moved failures still refuse absent live inputs. The parser has no unresolved entries, and the retained workspace result remains failed.

The [fifth workspace run](sweep-001/run-005/classification-002/report.md) at `7f93f9cb` exited 101 after 229.281 seconds. Its results, recorded in `0dab373a`, list 101 unchanged failure causes after the [approved WMS rename](sweep-001/run-005/classification-002/renamed-test.json). It reported 2,339 passes, including 84 explicit skips, plus six passing doctests and two filtered cases. The capture kept HEAD and all 29,110 tracked files unchanged. All 34 mapped app cases passed, and 17 earlier moved failures still refused absent live inputs. No cause or parser entry remains unresolved.
