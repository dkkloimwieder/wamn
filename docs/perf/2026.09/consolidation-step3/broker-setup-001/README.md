The shared event broker tests pass at b874ddc0bb6a813516d97f37d24ea7ac4e7b8370.

The run reports three passes, no failures, and no ignored tests in 35.079 seconds. The tests cover private credential files, scoped runtime permissions, and refusal of invalid declarations. Source bytes and file modes remain unchanged.

These tests read the generated broker configuration. Live NATS permission cases and the complete app cluster runs remain pending.
