# Recorded test run

Source: `5acbe42a7441b1186d9cf72648a387b800d4306c`.

The compiler reported the same missing object store extension trait. The preceding edit removed an unused import but did not add the required trait.

`record.json` names the actual commands, exit codes, and elapsed times. The adjacent logs contain the actual output.
