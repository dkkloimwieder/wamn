The workload and event broker tests pass at 609feea001692c230cbac756a1042c8fe96c8e4a.

The run reports eight passes, no failures, and no ignored tests in 2.136 seconds. It covers native workload observations and the scoped broker configuration. Source bytes and file modes remain unchanged.

The workload tests use saved Kubernetes responses and changed copies that must fail. Live cluster runs remain pending.
