The [Receiving postcommit pair](pair.json) passed at source `965cd41835bd6e28b3f24b080392dac2dc53213e`. The [command record](../receiving-postcommit-cargo-003/record.json) reports exit 0 after 2039.6780390350032 seconds, ending at `2026-09-11T22:05:25.234017+00:00`. Cargo reports one passed test, no failed or ignored tests, and 64 filtered tests. These are elapsed test times, not application performance measurements.

The [baseline verdict](baseline/verdict.json) and [additive verdict](additive/verdict.json) each pass all five retained test groups. They cover unchanged overlay routes, observed schema requirements, duplicate handler delivery, a native termination advisory after handler retry exhaustion, and independent event progress. Both [baseline telemetry](baseline/telemetry/result.json) and [additive telemetry](additive/telemetry/result.json) pass.

Each installation waited 122 seconds before duplicate delivery and preserved one approved inspection at row version 2. The [baseline materializer result](baseline/postcommit.json) and [additive materializer result](additive/postcommit.json) each record three actual blocked handler attempts. Each records one native `MSG_TERMINATED` advisory for its poison event with `deliveries: 1`. The three retries are handler attempts, not three broker deliveries. A later valid event produced one pending inspection at row version 1, with no inspection committed for the poison event. Progress took 16145 milliseconds for baseline and 15611 milliseconds for additive, within the retained 90-second limit.

The [baseline schema observation](baseline/overlay-compatibility.json) and [additive schema observation](additive/overlay-compatibility.json) retain the same 95 overlay files and component hash. Their initial base migrations and observed schema hashes differ. Both observations satisfy three tables, 14 fields, and seven constraints. The separate breaking candidate returns the exact `base-definition-mutation-refused` result without partial overlay state, then removes its database. Both observations retain `schema_admission_claim: false`. This is an executed schema observation, not a claim that production admission enforces that contract.

Both materializer results record successful lock release and materializer restoration. Each installation writes its passing verdict only after its owned resource cleanup succeeds. The pair then compares both completed installations and records `cleanup: pass`. The separate [post-run observation](../receiving-postcommit-cargo-003/post-run-observation.json) ran at `2026-09-11T22:07:16.971672+00:00`. All five observation commands exited 0, and both installations' clusters, containers, images, and private directories were absent. The warm checkout remained at the exact tested source with an empty Git status, and the existing `wamn` cluster remained present.

[The copy comparison](publication.json) records the hashes, sizes, and original modes of all 276 copied files, totaling 1226957 bytes. The files retain their original bytes and copied filesystem modes, including `0600` files. Git retains the executable mode bit, not all filesystem permission bits. The original component hash lists remain unchanged in [baseline](baseline/component-bytes.sha256) and [additive](additive/component-bytes.sha256).

This targeted pair does not execute the default startup, development-environment, or operator-recovery cases. It does not resolve the [startup certificate failure](../receiving-cluster-live-005/README.md) or declare native C or stage 3 complete. The earlier [image-build failure](../receiving-postcommit-cargo-001/README.md) and [launch-input failure](../receiving-postcommit-cargo-002/README.md) remain unchanged.
