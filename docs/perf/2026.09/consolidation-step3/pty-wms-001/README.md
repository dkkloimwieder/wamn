The [WMS terminal test](result.json) passed both response modes at source `47391bbc237f2e760f5cddb8b2e8f54a417ccc1f`. The [command](../pty-wms-command-001/command.json) exited 0 after [4.508828254 seconds](../pty-wms-command-001/record.json). Each mode sent one read and one move, sent nothing from spent controls, exited 0, and restored the terminal, while the partial mode displayed both the committed result and the failure.

The run used the actual WMS binary with synthetic HTTP and database responses, so the recorded row and movement values do not establish SQL commits, object storage writes, or platform execution. The [binary and source record](../pty-wms-command-001/README.md) states the build limit and unchanged inputs, and [publication.json](../pty-wms-command-001/publication.json) lists every original file with its copied bytes and mode.
