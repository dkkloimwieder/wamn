# RC application result — 2026-09-11

This [run](../rc-live-command-002/record.json) used `7f5fb77accf3382c406c067a52dc4e0e057e955c` and exited 1 after 287.954 seconds.
The image checks passed, then the [original result](result.json) recorded the administrator credential parser's refusal of a URL with a query string.
Both selected application tests remain incomplete, and the result still has `cleanup: null` while the separate [cleanup record](cleanup.json) reports success.
The original [79-entry hash list](evidence.sha256) and every raw file remain unchanged, with their copied modes of `0600` or `0664`.
The hash list covers the captured files and excludes this later note.
