The [complete default run](../receiving-cluster-cargo-007/record.json) at `ed16ee43f6486ee6f4961d64d6f1db99b23731f3` passed with exit zero after 671.426 seconds. The [ordinary operator recovery run](../receiving-cluster-cargo-007/ordinary/record.json) passed all four cases after 99.719 seconds, with no failures or ignored cases. That command filtered 63 unrelated cases. The [default result](result.json) records no failure.

The [Cargo output](../receiving-cluster-cargo-007/cargo.log) records passing results for all eight route protocol cases. Materializer checks reached a passing [telemetry result](telemetry/result.json). The [startup result](startup-burst/result.json) passed the retained protocol and trace assertions. The complete case also passed its environment-isolation and operator-recovery assertions.

The [operator recovery result](operator-recovery/result.json) records a 153.310-second scheduler outage against the required 150 seconds. Scheduler recovery completed in 63.459 seconds, and recovery after the deliberate operator restart completed in 34.574 seconds. The same three host processes remained present. All 69 [route samples](operator-recovery/route-summary.json) reported serving, with no non-success response.

This run recorded no automatic operator restart during the scheduler outage. The focused ordinary tests cover the new waiting-state assertion branch. This passing full scenario does not repeat run 006's automatic restart branch. The [earlier failure](../receiving-cluster-live-006/README.md) remains unchanged.

The [INFO logging check](../receiving-cluster-cargo-007/operator-info-check.json) found exactly one `-zap-log-level=info` argument on the saved initial operator Pod. None of the 12 saved operator log files contains a DEBUG record. These files include bounded log tails and failed reads for earlier containers. The check does not establish the contents of unrecorded logs, and every original log remains unchanged.

The [post-run observation](../receiving-cluster-cargo-007/post-run-observation.json) found no owned cluster, container, image, or private directory. The frozen `wamn` cluster remained present. All 29,958 [captured tracked files](../receiving-cluster-cargo-007/source-stability.json) retained their bytes and modes, and the tested checkout remained clean at its recorded commit. The [publication map](publication.json) preserves every original helper and controller file, with exact byte hashes and POSIX modes. The saved scheduler objects contain no private key data, and no temporary private files were copied.
