The extracted test modules run at a8675424 with ignored tests included. Run-plane tests report 75 passes. Provisioning tests report 37 passes. Claims tests report 39 passes and one failure, but nine reported passes return without exercising their optional subject. The failure requires an absent disposable PostgreSQL URL and matches the retained stage 2 baseline. See classified-results.json for each unexecuted or failed case. No live database result is claimed.
