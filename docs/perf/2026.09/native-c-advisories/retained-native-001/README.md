# Recorded test run

Source: `cd1b24b5d2131f8851aaff271b1af09cdc80acff`.

The retained advisory test and native runtime deduplication test passed against one private NATS 2.10.29 process. The extracted executor log redaction test also passed. The broker stopped and its private files were removed.

`record.json` names the actual commands, exit codes, and elapsed times. The adjacent logs contain the actual output.
