--- before/materializer-live.yaml
+++ after/materializer-live.yaml
@@ -56,9 +56,9 @@
       environment: wamn-receiving-journey
       hostInterfaces:
         # The host's hostInterfaces are the ALLOWLIST for host plugins. The
-        # materializer imports PostgreSQL, JetStream, the routing resource type
-        # borrowed by delivery, and the production router-delivery bridge; all
-        # four must be listed or the component fails to instantiate.
+        # materializer uses WAMN registration policy and the native named NATS
+        # binding for delivery. The host owns the events binding configuration
+        # and restricted credentials; the workload cannot override either.
         - namespace: wamn
           package: postgres
           version: "0.1.0"
@@ -66,7 +66,12 @@
         - namespace: wamn
           package: jetstream
           version: "0.1.0"
-          interfaces: [types, consumer]
+          interfaces: [types, registration]
+        - namespace: wasmcloud
+          package: nats
+          version: "0.1.0"
+          name: events
+          interfaces: [types, jetstream]
         - namespace: wamn
           package: flow-http-routing
           version: "0.1.0"
@@ -100,6 +105,6 @@
               WAMN_MAT_TENANT: receiving-route-auth
               WAMN_MAT_FETCH_MS: "500"
               WAMN_MAT_SWEEP_MS: "500"
-              # Retry is bounded; the final failed attempt is published to the
-              # host-derived per-registration DLQ before the source is termed.
+              # Retry is bounded. The broker retains exhaustion and termination
+              # advisories without a separate WAMN payload copy.
               WAMN_MAT_MAX_DELIVER: "5"
--- before/materializer-probe.yaml
+++ after/materializer-probe.yaml
@@ -56,9 +56,9 @@
       environment: warehouse-eu-3
       hostInterfaces:
         # The host's hostInterfaces are the ALLOWLIST for host plugins. The
-        # materializer imports PostgreSQL, JetStream, the routing resource type
-        # borrowed by delivery, and the production router-delivery bridge; all
-        # four must be listed or the component fails to instantiate.
+        # materializer uses WAMN registration policy and the native named NATS
+        # binding for delivery. The host owns the events binding configuration
+        # and restricted credentials; the workload cannot override either.
         - namespace: wamn
           package: postgres
           version: "0.1.0"
@@ -66,7 +66,12 @@
         - namespace: wamn
           package: jetstream
           version: "0.1.0"
-          interfaces: [types, consumer]
+          interfaces: [types, registration]
+        - namespace: wasmcloud
+          package: nats
+          version: "0.1.0"
+          name: events
+          interfaces: [types, jetstream]
         - namespace: wamn
           package: flow-http-routing
           version: "0.1.0"
@@ -100,6 +105,6 @@
               WAMN_MAT_TENANT: receiving-route-auth
               WAMN_MAT_FETCH_MS: "500"
               WAMN_MAT_SWEEP_MS: "500"
-              # Retry is bounded; the final failed attempt is published to the
-              # host-derived per-registration DLQ before the source is termed.
+              # Retry is bounded. The broker retains exhaustion and termination
+              # advisories without a separate WAMN payload copy.
               WAMN_MAT_MAX_DELIVER: "5"
--- before/measure/host-base.yaml
+++ after/measure/host-base.yaml
@@ -160,12 +160,52 @@
         - { name: WAMN_REGISTRY_AUTH_FILE, value: /etc/wamn/registry/config.json }
         - name: WAMN_COMPONENT_ARTIFACT_BASE
           value: registry.wamn-system.svc.cluster.local:5000/wamn/components
-        # [l5i9.17] The data-plane (event-plane) NATS for the wamn:jetstream
-        # plugin — the materializer Service workload's stream/doorbell path.
-        # Absent, the plugin still links and returns connection-unavailable.
+        # Host-owned registration and publication use their own broker user.
+        # The native materializer binding uses a separate restricted credential.
+        # Required Secrets keep password bytes outside workload configuration.
         - name: WAMN_EVT_NATS_URL
           value: nats://evt-nats.wamn-system.svc.cluster.local:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - name: WAMN_EVT_ORG
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: org
+              optional: false
+        - name: WAMN_EVT_PROJECT
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: project
+              optional: false
+        - name: WAMN_EVT_ENV
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: environment
+              optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -174,6 +214,12 @@
               - key: .dockerconfigjson
                 path: config.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
--- before/measure/host-overlay.yaml
+++ after/measure/host-overlay.yaml
@@ -56,6 +56,17 @@
           value: registry.probe.invalid:5000/proofs/components
         - name: WAMN_EVT_NATS_URL
           value: nats://nats.probe.invalid:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - { name: WAMN_EVT_ORG, value: acme }
+        - { name: WAMN_EVT_PROJECT, value: receiving }
+        - { name: WAMN_EVT_ENV, value: dev }
         - { name: WAMN_ORG, value: acme }
         - { name: WAMN_PROJECT, value: receiving }
         - { name: WAMN_SCHEMA, value: receiving }
@@ -84,6 +95,20 @@
               key: url
               optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -92,6 +117,12 @@
               - key: .dockerconfigjson
                 path: config.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
--- before/normal/host-base.yaml
+++ after/normal/host-base.yaml
@@ -160,12 +160,52 @@
         - { name: WAMN_REGISTRY_AUTH_FILE, value: /etc/wamn/registry/config.json }
         - name: WAMN_COMPONENT_ARTIFACT_BASE
           value: registry.wamn-system.svc.cluster.local:5000/wamn/components
-        # [l5i9.17] The data-plane (event-plane) NATS for the wamn:jetstream
-        # plugin — the materializer Service workload's stream/doorbell path.
-        # Absent, the plugin still links and returns connection-unavailable.
+        # Host-owned registration and publication use their own broker user.
+        # The native materializer binding uses a separate restricted credential.
+        # Required Secrets keep password bytes outside workload configuration.
         - name: WAMN_EVT_NATS_URL
           value: nats://evt-nats.wamn-system.svc.cluster.local:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - name: WAMN_EVT_ORG
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: org
+              optional: false
+        - name: WAMN_EVT_PROJECT
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: project
+              optional: false
+        - name: WAMN_EVT_ENV
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: environment
+              optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -174,6 +214,12 @@
               - key: .dockerconfigjson
                 path: config.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
--- before/normal/host-overlay.yaml
+++ after/normal/host-overlay.yaml
@@ -56,6 +56,17 @@
           value: registry.probe.invalid:5000/proofs/components
         - name: WAMN_EVT_NATS_URL
           value: nats://nats.probe.invalid:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - { name: WAMN_EVT_ORG, value: acme }
+        - { name: WAMN_EVT_PROJECT, value: receiving }
+        - { name: WAMN_EVT_ENV, value: dev }
         - { name: WAMN_ORG, value: acme }
         - { name: WAMN_PROJECT, value: receiving }
         - { name: WAMN_SCHEMA, value: receiving }
@@ -84,6 +95,20 @@
               key: url
               optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -92,6 +117,12 @@
               - key: .dockerconfigjson
                 path: config.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
--- before/wms/host-base.yaml
+++ after/wms/host-base.yaml
@@ -160,12 +160,52 @@
         - { name: WAMN_REGISTRY_AUTH_FILE, value: /etc/wamn/registry/config.json }
         - name: WAMN_COMPONENT_ARTIFACT_BASE
           value: registry.wamn-system.svc.cluster.local:5000/wamn/components
-        # [l5i9.17] The data-plane (event-plane) NATS for the wamn:jetstream
-        # plugin — the materializer Service workload's stream/doorbell path.
-        # Absent, the plugin still links and returns connection-unavailable.
+        # Host-owned registration and publication use their own broker user.
+        # The native materializer binding uses a separate restricted credential.
+        # Required Secrets keep password bytes outside workload configuration.
         - name: WAMN_EVT_NATS_URL
           value: nats://evt-nats.wamn-system.svc.cluster.local:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - name: WAMN_EVT_ORG
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: org
+              optional: false
+        - name: WAMN_EVT_PROJECT
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: project
+              optional: false
+        - name: WAMN_EVT_ENV
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: environment
+              optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -174,6 +214,12 @@
               - key: .dockerconfigjson
                 path: config.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
--- before/wms/host-overlay.yaml
+++ after/wms/host-overlay.yaml
@@ -56,6 +56,17 @@
           value: registry.probe.invalid:5000/proofs/components
         - name: WAMN_EVT_NATS_URL
           value: nats://nats.probe.invalid:4222
+        - name: WAMN_EVT_NATS_USERNAME
+          valueFrom:
+            secretKeyRef:
+              name: wamn-event-nats
+              key: username
+              optional: false
+        - { name: WAMN_EVT_NATS_PASSWORD_FILE, value: /etc/wamn/event-nats/password }
+        - { name: WAMN_MAT_NATS_BINDING_FILE, value: /etc/wamn/materializer-nats/binding.json }
+        - { name: WAMN_EVT_ORG, value: acme }
+        - { name: WAMN_EVT_PROJECT, value: wms }
+        - { name: WAMN_EVT_ENV, value: dev }
         - { name: WAMN_ORG, value: acme }
         - { name: WAMN_PROJECT, value: wms }
         - { name: WAMN_SCHEMA, value: wms }
@@ -88,6 +99,20 @@
               key: url
               optional: false
       volumes:
+        - name: event-nats
+          secret:
+            secretName: wamn-event-nats
+            optional: false
+            items:
+              - key: password
+                path: password
+        - name: materializer-nats
+          secret:
+            secretName: wamn-materializer-nats
+            optional: false
+            items:
+              - key: binding.json
+                path: binding.json
         - name: registry-pull
           secret:
             secretName: wamn-registry-pull
@@ -103,6 +128,12 @@
               - key: credentials.json
                 path: credentials.json
       volumeMounts:
+        - name: event-nats
+          mountPath: /etc/wamn/event-nats
+          readOnly: true
+        - name: materializer-nats
+          mountPath: /etc/wamn/materializer-nats
+          readOnly: true
         - name: registry-pull
           mountPath: /etc/wamn/registry
           readOnly: true
