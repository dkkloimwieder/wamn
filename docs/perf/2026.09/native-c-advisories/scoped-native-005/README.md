# Scoped native delivery and retention

Source: `48fba09768586c6ec1de3b3206710d8cf1d18d1d`.

The compiler refused the new expiry assertion because the stream update returns stream information directly. No native case ran.

The command returned 101 after 14.458 seconds. `record.json` records the command, and `cargo.log` retains the actual output.
