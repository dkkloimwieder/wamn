Prepared correction for wamn-0h0g.2.7.13, source ab0467f415dfd6b31e70323ec6ca049d5d2a5298.

The observed error is a production query/decoder mismatch. ACTIVE_WIRING_SQL returns six columns (wiring_resolution.rs:54–80); resolve_active_wiring passes that row to decode_released_wiring (:470–476), which reads complete manifest components and their count at indices 6 and 7 (:853–860). The trusted_http_route fixture seeds actual catalog tables and uses the real RouterDriver. The failure happens inside RouterDriver.execute during resolution, before guest invocation. The source file has the same git blob e9b58228ef78389166d2a960ec5b7011003863a2 at the 2.8 baseline dfa1c318 and at ab0467f4. Commit 1efc3b2c818c49ee2d7be38ca7d9b2445fe61b0b expanded the shared decoder and RELEASE_WIRING_SQL for complete operation dependencies but left ACTIVE_WIRING_SQL with six columns.

combined.patch contains both reviewed proposals. proposal.patch changes only ACTIVE_WIRING_SQL. It joins the snapshot for the selected head's exact tenant/release ID, checks its environment, and appends the complete manifest component projection and count already used by RELEASE_WIRING_SQL. The only change within that reused projection is its existing selected release-ID alias. The first six columns, five arguments, activation/version/confirmed-hash/tombstone selection, exact node bindings, and all decoder/cardinality checks remain. The added correlated values are grouped by the selected manifest. Frozen and candidate queries, grants, authority classes, and fixture operation facts are unchanged. This does not substitute node components for the full manifest dependency closure or change tests from Active to Frozen.

negative-test/proposal.patch adds two refusal cases to the existing virtualization sentinel test, before its first successful Active call can populate the cache. An owned release-2 head with exact package/node membership but no snapshot must return active-wiring-not-found. Adding release 2's complete canonical snapshot must then return active-wiring-effective-release-mismatch against the driver's mounted release 1. Restoring head 1 leaves the original sentinel, real connection, and HTTP panic assertions in place. The test derives release 2 from the existing validated manifest and uses its canonical bytes/digest. It changes no trigger, grant, or production hook. Both errors must come from the real driver; arbitrary failures do not pass.

The existing virtualization owner covers the actual positive Active execution and can carry these refusal cases without a new harness. The alternative executor_platform_surface_live owner executes raw SQL under its generation and includes tombstone and foreign-digest controls, but its module explicitly excludes the decoder/lowering boundary; its old flat component expectation at :388 is also separate fixture drift. It is not a replacement for this regression proof.

No repository source changed and no compiler, Cargo, PostgreSQL, registry, or live process ran during this preparation. static-validation.json records source scope, outer SELECT-list shape, exact reused closure columns, rustfmt and patch applicability. Its lexical checks are not a PostgreSQL execution result. virtualization-live-002 remains failed: artifact inspection passed one exact test; the sentinel test failed before guest invocation. Root must review/apply both patches after the active WMS owner releases the fixed source, then compile and rerun the existing virtualization wrapper on a new clean commit and new evidence directory. The wrapper rebuilds the proof profile, executes the same two exact test names with --include-ignored, owns and removes its PostgreSQL/OCI resources, and restores m1 before the next production journey.
