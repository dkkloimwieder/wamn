# Generated TUI recipe

This report records the combined recipe for `wamn-10yt.62.8`.
The source is `a0cfbdea802e15a0797b7aaa031c4c83aa11a6ef`.
That commit changes only the operator recipe and its Python runner.
It changes no Rust source, Cargo manifest, workspace member, or generated package.

The [combined invocation](run-001/result.json) passes all 11 stages in 504.105 seconds.
The source commit and clean working tree remain unchanged after the run.
The [integrity manifest](run-001/SHA256SUMS) covers 264 retained files.

## Recipe scope

The [operator recipe](../../../operations/build-and-test.md#generated-tui-generated-operator-terminals) documents package-owned outputs, launch, session replacement, and Rust customization.
The runner uses the clean source worktree and its existing build target.
It keeps new evidence outside that source tree.
The invocation is:

```bash
python3 docs/perf/2026.09/generated-tui-recipe/tools/run.py \
  --tree "$PWD" \
  --evidence-dir /home/kaalin/dev/wamn/docs/perf/2026.09/generated-tui-recipe/run-001
```

The runner regenerates Receiving, its Acme overlay, and WMS against fresh PostgreSQL databases.
It builds the required native binaries and runs the shared client, Receiving, and emitter tests.
It scaffolds `receiving.record_receipt`, runs its declared tests, and regenerates again with that developer-owned crate present.
It requires unchanged scaffold sources and removes only the owned temporary crate.

The live stages use three separate disposable environments.
The generated-terminal stage drives `--watch --tui receiving` without `--hold`, changes source, observes restart, and exits through the terminal.
The Receiving stage drives the reference composition through refusal, correction, stored receipt, and entry reset.
The WMS stage drives both a successful move and a committed move whose label store fails.
Each stage retains its own results and cleanup evidence.

The [command receipts](run-001/commands.json) record every command, exit code, and duration.
The [materialization receipt](run-001/materialize/result.json) records all three packages, zero changed generated paths, and complete container cleanup.
The [second generation](run-001/regenerate-with-scaffold/result.json) also leaves generated bytes unchanged.
The [scaffold receipt](run-001/scaffold-result.json) records unchanged developer-owned sources after generation.
The shared client and Receiving tests pass 184 tests, the emitter passes 11, and the scaffold passes three.
These 198 tests report no failures, ignored tests, or filtered tests.

The [operator session](run-001/operator-loop/operator-result.json) proves a real read, source-triggered restart, a different target instance, and empty replacement state.
Its [environment receipt](run-001/operator-loop/result.json) records source restoration, closed listeners, removed containers, and reaped processes.
The [Receiving session](run-001/receiving-composition/client/result.json) preserves the refused entry, accepts correction, stores the selected location, and preserves decimal scale.
It omits the blank line, spends the successful entry, restores the terminal, and removes its database fixtures.
The [Receiving environment](run-001/receiving-composition/result.json) also passes cleanup.

The [successful WMS session](run-001/wms-composition/generated-tui-success/result.json) displays the stored label key.
The journey compares the stored object with the returned label bytes.
The [partial WMS session](run-001/wms-composition/generated-tui-partial/result.json) displays the committed movement beside the observed label failure.
Each session sends exactly one read and one move.
Retry and submit controls send nothing after the command is spent.
Both sessions restore the terminal and remove their database fixtures.
The [journey cleanup](run-001/wms-composition/cleanup.receipt) removes the owned cluster, containers, reader process, and host image.

## Eight implementation children

These records identify the implementation sequence and its retained evidence.
Earlier receipts establish results for their named sources, not for the current combined invocation.

| Child | Implementation | Retained evidence |
| --- | --- | --- |
| `wamn-10yt.62.1` | P1 extends the landed `.45` projection at `784cade7`. | The [WMS workspace log](../generated-tui-wms/workspace-001/workspace.log) retains passing contract projection and `client_ir` tests. |
| `wamn-10yt.62.2` | P2 adds validated requests and shared submission state at `b7986bc6` and `43729af3`. | The [Receiving focused log](../generated-tui-parity/focused-002/command.log) and [WMS focused result](../generated-tui-wms/focused-003/result.json) cover request bytes and submission outcomes. |
| `wamn-10yt.62.3` | The emitter creates package-owned terminals at `495cffb9`. | The [integration materialization](../generated-tui-integration/runs/materialize-001/evidence/result.json) and [emitter log](../generated-tui-parity/gates-001/command.log) retain deterministic output and screen assertions. |
| `wamn-10yt.62.4` | The loop supervises generated sessions at `1fcdf8db`. | The [integration live result](../generated-tui-integration/runs/live-001/evidence/result.json) retains restart, unchanged source, and owned cleanup. |
| `wamn-10yt.62.5` | Scaffolding creates ordinary Rust overrides at `2ad19106`. | The [WMS workspace log](../generated-tui-wms/workspace-001/workspace.log) retains passing scaffold creation, generated fallback, and CLI parser tests. |
| `wamn-10yt.62.6` | Receiving composes generated screens and deletes the old implementation at `9e3307eb`. | The [parity report](../generated-tui-parity/report.md) links interaction tests, exact wire bytes, a negative control, and the live terminal proof. |
| `wamn-10yt.62.7` | WMS composes the move form at `e0ea0de3`, with evidence retained by `826037ff`. | The [WMS report](../generated-tui-wms/report.md) links the live label object, committed partial response, request counts, and revision negative control. |
| `wamn-10yt.62.8` | The combined recipe and runner are `a0cfbdea`. | The [combined result](run-001/result.json) passes all 11 stages with unchanged source and completed cleanup. |

The WMS proof uses the platform response contract implemented under `wamn-b2m6.10`.
The [WMS report](../generated-tui-wms/report.md) names that source and records both real terminal sessions.
The shared reducer owns the response classification, including uncertainty when commitment evidence is absent.

## Workspace baseline and proof limits

The latest retained [workspace result](../generated-tui-wms/workspace-001/workspace-results.json) belongs to the WMS source, before this recipe commit.
It reports 2,183 passing tests, six passing doctests, and 74 failures across 34 targets.
The [comparison](../generated-tui-wms/workspace-001/baseline-comparison.json) preserves all 74 prior failure identities and classified causes.
It records no added failures, removed failures, or changed causes.

That sweep includes ignored tests and excludes the two documented schema-regeneration tests.
At least 85 reported passes skip their live or artifact proof.
The separately armed WMS journey passes its live assertions.
This recipe adds no source manifest and makes no new workspace sweep claim.

Database counts prove committed effects, not the number of HTTP attempts.
The WMS relay counts requests and retains their bodies separately.
The live partial result proves an observed store failure after commitment.
It does not prove every timeout or response-loss outcome.

The [Receiving report](../generated-tui-parity/report.md) and [WMS report](../generated-tui-wms/report.md) retain their failed increments beside later successful runs.
Those failures remain failures and do not become passing receipts after a fix.
The combined recipe likewise requires a new evidence directory for another invocation.
A passing combined result does not establish full release readiness.
The separate intermittent executable fixture finding remains open as `wamn-10yt.62.9`.
Its investigation falls outside the eight implementation children.
