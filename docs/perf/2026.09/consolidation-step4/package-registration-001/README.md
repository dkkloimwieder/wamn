Package registration now uses Rust decisions and plain SQL in the existing transaction. Source commit: `ac2e3649a9c316c65de2ead70b326fd9b8ca5796`. Test correction: `33a6b60a514b124a5e4e71de872ad2f0fda4fd10`. The test source started from `510e4fbf22d8ab321ef3cff1eabc69cf487a3cce` plus the captured changes.

The change removes `catalog.register_package` from both schema declarations. The existing transaction lock still serializes each tenant and package lineage. PostgreSQL retains sealing, row locks, uniqueness, immutable rows, and tenant isolation. Exact replay keeps the first timestamp and reports no insertion. The [source comparison](final-checks.json) matches the final committed bytes with the [last test snapshot](../package-registration-002/source.json).

[Eight package-planner tests passed](command-1.log), followed by [one ordinary CLI test](ordinary-ctl-controller.log). [The native targets compiled successfully](build-results.json), including the [control-store target](control-build-result.json). Each build used Rust 1.98.0, debug mode, two jobs, and this isolated worktree's own target. The build records contain the exact commands, elapsed times, and exit codes.

[The registration test passed](registration.log) against both schema installations on PostgreSQL 18.6. It exercised concurrent exact replay, conflicting content, wrong successors, predecessor refusals, first timestamps, and rollback. [Release sealing and attestation passed](publish-release.log). [Both control-store tests passed](portable-store.log), including the control-author tenant and privilege assertions. These runs used fresh disposable PostgreSQL instances.

[The first application run failed](apply-package.log) before its later migration assertions. A retained grant assertion expected 12 rows and found 16. The current Receiving manifest declares eight public operations, while two old assertions expected six per package. The separate test correction compares exact permission sets through the existing manifest helper. It preserves the sibling-isolation and denial assertions.

[The complete application rerun passed](../package-registration-002/apply-package.log) on another fresh PostgreSQL instance. It reached migration-byte drift, suffix rollback, predecessor-prefix refusal, sealing, and upgrade replay. [Its test command took 1.612500 seconds](../package-registration-002/apply-package.json). [The enclosing PostgreSQL command took 4.396663 seconds](../package-registration-002/postgres-result.json). The earlier failed run remains unchanged.

The final runs passed 14 scoped cases. [All five disposable PostgreSQL instances were removed](final-checks.json), including the failed application's instance. The [publication map](publication-map.json) preserves file hashes, sizes, and POSIX modes. The capture scan found no private-key material, credential URLs, or bearer headers. These results cover the selected cases and do not claim a workspace test run or application-cluster result.
