The metadata rename passed 69 existing cases and retained all 248 generated app files. This result belongs to `wamn-47wm.5.5`.

The mechanical rename commit is `e7976eaffe1043af852013449ac9286c33447931`. The separate test commit is `80d9af2a7c71a51834eff8e1f9c133ecbd6df4e1`. Their base is `daae50fe83ba85bb921a8be82b0e6fa566cde0e2`. [The rename record](rename.json) lists the seven files and exact name changes. [The commit record](source-commit.json) matches each commit to its tested source. Generated filenames, serialized fields, refusal text, and application pins retain their original values.

Serde attributes preserve the existing structure name and wrong-type error. [The original native call](error-before-001/command-3.stdout) and [the renamed native call](error-after-001/command-3.stdout) returned identical bytes. The original call used all 19 unchanged generator library source files from the base. The [error comparison](error-comparison.json) records both calls and removal of their two temporary programs. The test commit adds assertions to the existing canonical-reader case. [That selected case](contract-test-001/commands.json) passed in 1.416 seconds with 26 other generation cases filtered.

[The final scoped run](focused-002/commands.json) passed 27 generator cases, five parity cases, 33 Receiving cases, two materialize cases, and two control cases. It ignored no tests. All commands used Rust 1.98.0, offline locked dependencies, two build jobs, and this lane's own target. The source stayed unchanged during each command sequence. [The earlier run](focused-001/commands.json) also passed before the explicit Serde attributes were added. The selected control cases exercised release-policy refusal and exact package SQL loading.

Both app comparisons used the retained `materialize_package check` path and fresh PostgreSQL 18.6 databases. The [original command](before-apps-command-001/result.json) passed in 4.150 seconds, and the [final command](after-apps-command-001/result.json) passed in 4.654 seconds. Both used the actual app migrations and fixed generator provenance. [The comparison](comparison.json) covers 93 Receiving files, 66 Acme files, and 89 WMS files. Every generated byte remained unchanged. The same materializer binary remained after the separate test assertion.

Both database logs record the removal of their owned temporary PostgreSQL cluster. The captures did not independently inspect the removed directories. No existing cluster was used, and the records contain no private database connection values. Large source maps use gzip with a zero timestamp in the published copy. `publication.json` records the original bytes, modes, hashes, and stored paths. Root originals remain intact for comparison, and the integrated workspace sweep remains separate.
