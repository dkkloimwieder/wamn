[package]
name = "wamn-scenario-catalog"
description = "Persistence queries and run-to-scenario transforms for the product scenario catalog."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde_json = "1"
wamn-node-invoke = { path = "../../node/invoke" }
wamn-run-state = { path = "../../execution/run-state" }
wamn-scenario-model = { path = "../../scenarios/model" }
