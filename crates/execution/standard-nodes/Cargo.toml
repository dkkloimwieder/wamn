[package]
name = "wamn-standard-nodes"
description = "Standard-node palette metadata retained for flow authoring."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# The canonical pure execution and publication contract for standard nodes.
wamn-execution-contract = { workspace = true }
