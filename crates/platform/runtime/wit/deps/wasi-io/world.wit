package wasi:io@0.2.12;

@since(version = 0.2.0)
world imports {
    @since(version = 0.2.0)
    import error;

    @since(version = 0.2.0)
    import poll;

    @since(version = 0.2.0)
    import streams;
}
