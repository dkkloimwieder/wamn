
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Rust build artifacts
/target
/components/target
*.wasm

# Client workspace artifacts
/node_modules/
/clients/*/node_modules/
/clients/*/dist/
# Vitest browser mode captures a screenshot for every failing test; they are
# evidence of one run, not of the source.
/clients/*/.vitest-attachments/
__screenshots__/
