`apps/wamn_wms/` owns package `wamn_wms`, guest component `wms`, data crate `wamn-wms-data-access`, and generated native UI crate `wamn-generated-wms-tui`.

The WMS example lives in `examples/wms_move.rs`.
The `wamn-client-terminal` package runs it through the `wms_move` Cargo example target.
