SELECT product_id, location_id, disposition, sum(quantity) AS quantity, count(DISTINCT packaging_id)::integer AS packaging_count FROM inventory WHERE lifecycle = 'open' GROUP BY product_id, location_id, disposition ORDER BY product_id, location_id, disposition;
