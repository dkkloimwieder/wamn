UPDATE inventory SET packaging_id = $2, location_id = $3, row_version = row_version + 1 WHERE id = $1 RETURNING id, product_id, packaging_id, location_id, quantity, disposition, lifecycle, row_version;
