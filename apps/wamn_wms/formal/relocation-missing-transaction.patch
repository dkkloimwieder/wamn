--- a/model.rs
+++ b/model.rs
@@ -430,6 +430,9 @@
     };
     for id in [false, true] {
         if !affected(from_state.inventory, command.action, id) {
+            continue;
+        }
+        if matches!(command.action, Action::RelocatePackaging { .. }) && id {
             continue;
         }
         let from_item = from_state.inventory[usize::from(id)];
