# S5 log pipeline bridge. The wamn-host OTLP log exporter is gRPC/tonic; Loki's
# OTLP ingest is HTTP — so a collector is REQUIRED between them. This one
# receives OTLP/gRPC on :4317 and forwards to Loki's native OTLP endpoint over
# otlphttp. Its own telemetry (otelcol_exporter_sent_log_records, …) is exposed
# on :8888 for the logbench loss cross-check.
#
#   kubectl -n wamn-system apply -f deploy/infra/otel-collector.yaml
#   kubectl -n wamn-system rollout status deploy/otel-collector --timeout=120s
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: wamn-system
data:
  config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
    processors:
      batch:
        send_batch_size: 8192
        send_batch_max_size: 16384
        timeout: 200ms
    exporters:
      otlphttp/loki:
        # Loki native OTLP; otlphttp appends /v1/logs to this base.
        logs_endpoint: http://loki:3100/otlp/v1/logs
        tls:
          insecure: true
        sending_queue:
          enabled: true
          queue_size: 10000
        retry_on_failure:
          enabled: true
      # [9.1] traces → Tempo's OTLP-gRPC distributor (deploy/infra/tempo.yaml).
      otlp/tempo:
        endpoint: tempo:4317
        tls:
          insecure: true
        sending_queue:
          enabled: true
          queue_size: 10000
        retry_on_failure:
          enabled: true
      # [9.8] RECEIVED OTLP app metrics (wamn.*) re-exposed as Prometheus text on
      # :8889 for metricbench to scrape. add_metric_suffixes:false keeps the OTel
      # names verbatim (dots→underscores only: wamn.run.executions →
      # wamn_run_executions), so the gate greps stable names. This is distinct
      # from the collector's OWN telemetry on :8888 (otelcol_* under
      # service.telemetry.metrics).
      prometheus:
        endpoint: 0.0.0.0:8889
        add_metric_suffixes: false
    service:
      telemetry:
        metrics:
          readers:
            - pull:
                exporter:
                  prometheus:
                    host: 0.0.0.0
                    port: 8888
      pipelines:
        logs:
          receivers: [otlp]
          processors: [batch]
          exporters: [otlphttp/loki]
        # [9.1] host spans → Tempo.
        traces:
          receivers: [otlp]
          processors: [batch]
          exporters: [otlp/tempo]
        # [9.8] host/service app metrics → Prometheus text on :8889.
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [prometheus]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
  namespace: wamn-system
spec:
  replicas: 1
  selector:
    matchLabels: { app: otel-collector }
  template:
    metadata:
      labels: { app: otel-collector }
    spec:
      containers:
        - name: otel-collector
          image: otel/opentelemetry-collector-contrib:0.115.1
          args: ["--config=/etc/otelcol/config.yaml"]
          ports:
            - { name: otlp-grpc, containerPort: 4317 }
            - { name: metrics, containerPort: 8888 }
            # [9.8] received app metrics (wamn.*) as Prometheus text.
            - { name: app-metrics, containerPort: 8889 }
          volumeMounts:
            - { name: config, mountPath: /etc/otelcol }
          resources:
            # No CPU limit (CFS-quota tail artifact — the S2 lesson).
            requests: { cpu: "1", memory: "256Mi" }
            limits: { memory: "1Gi" }
      volumes:
        - name: config
          configMap: { name: otel-collector-config }
---
apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: wamn-system
spec:
  selector: { app: otel-collector }
  ports:
    - { name: otlp-grpc, port: 4317, targetPort: 4317 }
    - { name: metrics, port: 8888, targetPort: 8888 }
    # [9.8] app-metrics scrape (wamn_*) for metricbench.
    - { name: app-metrics, port: 8889, targetPort: 8889 }
