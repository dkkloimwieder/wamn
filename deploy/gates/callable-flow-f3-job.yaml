# Exact-image gate for the callable-flow F3 graph, attachment, and recovery proof.
#
# Build and load the issue commit's gates image before applying this immutable Job:
#   docker build --target gates -t wamn-gates:cf-f3-<commit> .
#   kind load docker-image wamn-gates:cf-f3-<commit> --name wamn
#   sed "s/wamn-gates:cf-f3-ISSUE/wamn-gates:cf-f3-<commit>/" \
#     deploy/gates/callable-flow-f3-job.yaml | kubectl -n wamn-system apply -f -
apiVersion: batch/v1
kind: Job
metadata:
  name: callable-flow-f3
  namespace: wamn-system
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: callable-flow-f3
    spec:
      restartPolicy: Never
      containers:
        - name: proof
          image: wamn-gates:cf-f3-ISSUE
          imagePullPolicy: Never
          args: ["--log-level", "error", "callable-f3"]
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              memory: "128Mi"
