# Exact-image gate for the callable-flow F4 event, child, review, and callback proof.
#
# Build and load the issue commit's gates image before applying this immutable Job:
#   docker build --target gates -t wamn-gates:cf-f4-<commit> .
#   kind load docker-image wamn-gates:cf-f4-<commit> --name wamn
#   sed "s/wamn-gates:cf-f4-ISSUE/wamn-gates:cf-f4-<commit>/" \
#     deploy/gates/callable-flow-f4-job.yaml | kubectl -n wamn-system apply -f -
apiVersion: batch/v1
kind: Job
metadata:
  name: callable-flow-f4
  namespace: wamn-system
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: callable-flow-f4
    spec:
      restartPolicy: Never
      containers:
        - name: proof
          image: wamn-gates:cf-f4-ISSUE
          imagePullPolicy: Never
          args: ["--log-level", "error", "callable-f4"]
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              memory: "128Mi"
