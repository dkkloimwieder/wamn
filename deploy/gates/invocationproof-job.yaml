# wamn-5wd1.72 exact-image gate. The gates image and production run-worker image
# receive the same component-builder output; this job drives that exact
# /bench/flowrunner.wasm through ExecutionHost::execute_claimed against a
# throwaway database, including instantiation failure, stale-fence refusal,
# expired-lease recovery, stored response, and dequeue.
apiVersion: batch/v1
kind: Job
metadata:
  name: invocationproof
  namespace: wamn-system
spec:
  backoffLimit: 0
  template:
    metadata:
      labels: { app: invocationproof }
    spec:
      restartPolicy: Never
      containers:
        - name: invocationproof
          image: wamn-gates:dev
          imagePullPolicy: IfNotPresent
          args:
            - "--log-level"
            - "error"
            - "invocationproof"
            - "--flowrunner"
            - "/bench/flowrunner.wasm"
            - "--database-url"
            - "postgres://wamn_app:wamn_app@postgres.wamn-system.svc.cluster.local:5432/postgres"
            - "--admin-database-url"
            - "postgres://$(PGUSER):$(PGPASSWORD)@$(PGHOST):$(PGPORT)/postgres"
          env:
            - name: PGUSER
              valueFrom: { secretKeyRef: { name: postgres-fixture-superuser, key: username } }
            - name: PGPASSWORD
              valueFrom: { secretKeyRef: { name: postgres-fixture-superuser, key: password } }
            - name: PGHOST
              valueFrom: { secretKeyRef: { name: postgres-fixture-superuser, key: host } }
            - name: PGPORT
              valueFrom: { secretKeyRef: { name: postgres-fixture-superuser, key: port } }
          resources:
            requests: { cpu: "1", memory: "512Mi" }
            limits: { memory: "1Gi" }
