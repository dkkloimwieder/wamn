# Scoped native broker test

Source: `f1a4ec44`.

The native test passed. It exercised three environments, 18 runtime management refusals, eight observer refusals, and six runtime refusals across environments. It also exercised delivery, retained advisories, and refusal of changed stream and consumer declarations.

The command returned 0 after 22.387 seconds. Source files stayed unchanged during the run.

`record.json` names the exact command and native binary. `cargo.log` contains the actual output.
