The schema generation test passes at 090bbb671b9e29d5a9f0a7f57dd7be509bfdeb17.

The command runs one test and exits 0 in 48.347 seconds. The generated schema adds four required event fields for paired credentials, stream copies, and the duplicate window. Commit a0eb680d contains the generated bytes.
