diff --git a/services/identity/src/session.rs b/services/identity/src/session.rs
index 7de2e6e6..b8da0c43 100644
--- a/services/identity/src/session.rs
+++ b/services/identity/src/session.rs
@@ -210,7 +210,7 @@ async fn mint_for_principal(
         jti,
     };
     let mut signing = inner.signing.as_ref().ok_or_else(failed)?.lock().await;
-    sign_session_token(&mut signing.client, claims, started_at)
+    sign_session_token(&mut signing.client, claims, unix_seconds().ok_or_else(failed)?)
         .await
         .map_err(|_| failed())
 }
