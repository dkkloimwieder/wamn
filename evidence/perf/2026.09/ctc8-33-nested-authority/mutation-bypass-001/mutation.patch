diff --git a/crates/platform/runtime/src/plugins/connection_http.rs b/crates/platform/runtime/src/plugins/connection_http.rs
index c4e57e4c..73df4ae5 100644
--- a/crates/platform/runtime/src/plugins/connection_http.rs
+++ b/crates/platform/runtime/src/plugins/connection_http.rs
@@ -521,9 +521,7 @@ pub(crate) fn authorize_release_closure(
     });
     if !operation_admitted
         || !manifest.wirings.contains(&wiring)
-        || !root.is_some_and(|root| {
-            released_operation_reachable(manifest, root, &origin.operation, invocation)
-        })
+        || root.is_none()
     {
         return Err(ConnectionError::AttestationInvalid);
     }
