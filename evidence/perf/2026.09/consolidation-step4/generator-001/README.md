The generator split passed 67 tests and retained all 248 generated app files. This result belongs to `wamn-47wm.5.3`.

Source commit `155c26dfa0d388336d354e11de1470f1765ee5cd` extracts three private modules from `generate.rs`. Its base is `510e4fbf22d8ab321ef3cff1eabc69cf487a3cce`. All 101 top-level function bodies retain their exact bytes. Public declarations, their implementations, shared helpers, and constants remain in the parent. [The extraction record](extraction.json) lists every function and source hash. [The source commit record](source-commit.json) matches the committed files to the tested files.

[The final focused run](focused-002/commands.json) passed 27 generator cases, five parity cases, 33 Receiving cases, and two materialize cases. It ignored no tests and filtered 50 unrelated unit cases. Its four commands took 2.408, 2.231, 12.489, and 0.800 seconds. The fourth command rebuilt the retained materializer example. All commands used Rust 1.98.0, offline locked dependencies, two build jobs, and this lane's own target. [The first run](focused-001/commands.json) also passed and retains the removed unused-import warning.

The original and split generators each ran the existing `materialize_package check` path against fresh PostgreSQL 18.6 databases. Each run applied the actual Receiving, Acme, and WMS migrations. The fixed generation provenance was `wamn-schema-generator/0.1.0` with `rust-1.98.0`. [The byte comparison](comparison.json) covers 93 Receiving files, 66 Acme files, and 89 WMS files. Both runs retained every generated byte and application pin. Only the four intended generator source files changed.

[The original database command](before-apps-command-001/result.json) passed in 3.714 seconds. [The final database command](after-apps-command-001/result.json) passed in 5.010 seconds. Both logs record the removal of their owned temporary PostgreSQL cluster. The captures did not independently inspect the removed directories. No existing cluster was used, and no private database connection value appears in the records.

Large source maps use gzip with a zero timestamp in the published copy. `publication.json` records each original hash, byte count, mode, and stored path. Decompression restores the exact original bytes. The root capture directories remain unchanged for comparison. This focused result does not replace the integrated workspace sweep.
