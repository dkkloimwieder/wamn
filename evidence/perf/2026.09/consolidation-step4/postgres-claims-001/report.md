The first attempt stopped during dependency compilation. A static read found that the moved tests needed an explicit `VerifiedStatement` import.

The controller exited 130 after the targeted interruption. It stopped its PostgreSQL 18.6 server and recorded unchanged source bytes and modes. No Rust test outcome came from this attempt.

[The interruption record](interruption.json) names the exact owned processes. [The next attempt](../postgres-claims-002/report.md) used the corrected import. The original command and compiler output remain in this directory.
