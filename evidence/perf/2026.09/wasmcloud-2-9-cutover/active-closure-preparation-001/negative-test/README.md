# Active release-closure negative controls

This proposal changes only `tests/integration/src/virtualized_std_guest.rs` at source `ab0467f4`. It adds two checks to the existing live guest test before its first successful Active lookup. The existing positive environment, connection, and panic assertions remain byte-for-byte unchanged.

The owned admin connection copies release 1 package membership and the exact node binding to release 2, then selects head 2. Without snapshot 2, the real driver must return exactly `active-wiring-not-found`; this catches an incorrect join to release 1's existing snapshot. The test then clones the mounted typed manifest, sets `EffectiveReleaseId::new(2)`, canonicalizes and validates it with `ReleaseManifestWeld::load_canonical_bytes`, and inserts its own digest and bytes as snapshot 2. This request must return exactly `active-wiring-effective-release-mismatch`, demonstrating that closure decoding succeeded far enough to reject a release other than the mounted release. Finally, it restores the owned head to release 1 before the original positive requests.

The fixture uses its existing admin URL and production catalog contracts. Package and component facts precede the snapshot, as the seal trigger requires. There are no grant changes, disabled triggers, cache hooks, timing waits, new tests, or added guest runtime paths. Both exact errors precede the existing cache insertion, so no invalidation is needed.

Source anchors: `trusted_http_route.rs:402` owns the admin connection convention and `:511` / `:570` seed the existing release facts; `catalog-schema.sql:334` defines the mutable head FK, `:616` defines node bindings, `:650` defines snapshot digest validation, and `:664` seals component insertion; `package.rs:151` defines the typed release ID; `serving_manifest.rs:226` / `:265` provide manifest cloning and canonical bytes; `release_manifest.rs:178` validates the canonical manifest and computes its digest; `router_driver.rs:1119` resolves Active wiring and returns the two errors before cache insertion at `:1136`.

`static-validation.json` records source hash checks, surgical-diff checks, direct rustfmt, and read-only `git apply --check`. Compilation and live execution were not run. Apply this proposal together with the parent-owned Active SQL closure fix; the known six-column production SQL cannot satisfy these controls. Existing live test inputs and the owning serialized invocation remain unchanged.
