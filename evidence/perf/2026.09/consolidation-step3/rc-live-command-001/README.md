# RC command result

The native RC command exits 1 at `cc729d9e` after 418.801 seconds.
The command builds its debug binary and images before the runtime image digest assertion fails.
The adjacent `rc-live-001` directory retains the failure and cleanup records.
