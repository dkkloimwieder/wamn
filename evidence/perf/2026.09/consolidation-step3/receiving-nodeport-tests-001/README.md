# Receiving readiness tests

Both focused tests pass at `0bd0c089`. The command exits 0 after 98.068 seconds.
The tests refuse incorrect HTTP responses and unrelated connection errors. The full Receiving rerun remains separate.
