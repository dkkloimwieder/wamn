The generation test run passed all 60 tests at source `f5ad982d7ca8bd3598bb617e89990ef3d535744d`. Receiving ran 33 tests, and the generator ran 27 tests, with no failures or ignored tests. The command exited 0 after 5.287851892004255 seconds. [command.json](command.json), [record.json](record.json), and [cargo.log](cargo.log) retain the exact inputs, timing, and output. This run covered the two ordinary generation targets. It did not execute cluster cases.

[scope.json](scope.json) records the original partition at `965cd41835bd6e28b3f24b080392dac2dc53213e`. [body-comparison.json](body-comparison.json) records all 89 functions, their destinations, and their hashes from extraction commit `b406a56b8090364b565ba24961a84a9d9eca1ac5`. [source-correspondence.json](source-correspondence.json) shows that all four final file hashes match the tested commit. The original source hash also matches the tested commit's parent. The three source hashes in [verified-parse-record.json](verified-parse-record.json) match those same tested files. These supporting files retain their original source references.

The comparison records 88 unchanged function bodies. Only `generic_custom_operation_path_preserves_shipped_receiving_bytes` changes its body, from `.join("../../../apps/wamn_receiving")` to `.join("..")`. Receiving constants also use paths relative to their new app directory. The manifest change registers the existing crate's `generation` test target. The source comparison checks every recorded body hash and all 60 passing test names against the completed Cargo log.
