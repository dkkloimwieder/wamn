The [full default run](../receiving-cluster-cargo-006/record.json) at `fb6c2b2e28fbcb90a8a373672a60e2397488cae5` exited 101 after 574.364 seconds. Cargo reports 573.74 seconds for its one failed case. The [ordinary startup tests](../receiving-cluster-cargo-006/ordinary/record.json) passed all five cases, with no failures or ignored cases, after 104.012 seconds. That command filtered 61 unrelated cases.

The [Cargo output](../receiving-cluster-cargo-006/cargo.log) records passing results for all eight route protocol cases. The materializer checks reached a passing [telemetry result](telemetry/result.json). The corrected [startup test](startup-burst/result.json) also passed its protocol and trace checks. Its [protocol record](startup-burst/protocol.json) contains eight cold starts and eight warm starts, with a native start limit of four. The local host exited zero without forced termination, and its cleanup error count is zero.

The [new scheduler certificate](startup-burst/scheduler-certificate-ready.json) uses the existing ClusterIssuer, the four declared NATS DNS names, and IP address `127.0.0.1`. The [Deployment change](startup-burst/scheduler-deployment-ready.json) points its certificate volume at that separate Secret. The test supplies the existing runtime and operator client certificates, with normal peer verification and mutual TLS enabled. The saved Certificate and Deployment objects contain no private key data.

The [default result](result.json) later failed with `startup refusal must identify the observed container start`. The [operator records](operator-recovery/phases.json) show that execution stopped during the scheduler outage checks. The later recovery and final default assertions did not complete. This result establishes neither full default success nor completion of stage 3 or native C.

The [post-run observation](../receiving-cluster-cargo-006/post-run-observation.json) found no owned cluster, container, image, or private directory. The frozen `wamn` cluster remained present. All 29,110 [captured tracked files](../receiving-cluster-cargo-006/source-stability.json) retained their bytes and modes, and the tested checkout remained clean at its recorded commit. The [publication map](publication.json) preserves every original helper and controller file, with exact byte hashes and POSIX modes. No temporary private files were copied.
