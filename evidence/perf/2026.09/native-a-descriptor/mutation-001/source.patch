diff --git a/services/executor/src/lib.rs b/services/executor/src/lib.rs
index fa962f31..3e3fd38f 100644
--- a/services/executor/src/lib.rs
+++ b/services/executor/src/lib.rs
@@ -347,6 +347,14 @@ fn executor_credentials(
 }
 
 pub async fn run(args: ExecutorArgs) -> anyhow::Result<()> {
+    let descriptor_soft_limit = Some(256_usize);
+    tracing::info!(
+        ?descriptor_soft_limit,
+        default_max_connections = wash_runtime::host::quota::default_max_connections(),
+        default_max_http_ingress_connections =
+            wash_runtime::host::quota::default_max_http_ingress_connections(),
+        "native descriptor limits initialized"
+    );
     wash_runtime::init_crypto();
 
     // Installed once, before anything can pull, and the call that validates the
diff --git a/services/host/src/host.rs b/services/host/src/host.rs
index 0cc3e7a0..8e9e0a44 100644
--- a/services/host/src/host.rs
+++ b/services/host/src/host.rs
@@ -498,6 +498,14 @@ fn session_verifier(
 
 pub async fn run(args: HostArgs) -> anyhow::Result<()> {
     let startup_started = Instant::now();
+    let descriptor_soft_limit = Some(256_usize);
+    tracing::info!(
+        ?descriptor_soft_limit,
+        default_max_connections = wash_runtime::host::quota::default_max_connections(),
+        default_max_http_ingress_connections =
+            wash_runtime::host::quota::default_max_http_ingress_connections(),
+        "native descriptor limits initialized"
+    );
     wash_runtime::init_crypto();
 
     // Trust roots are a property of the host, not of any one pull, so they are
