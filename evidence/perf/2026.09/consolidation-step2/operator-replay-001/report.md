The [recorded command](command.txt) exited 0 with [these results](result.json).
It used the current helper and a retained Receiving [terminal capture](../../generated-tui-parity/live-002/environment/client/terminal.ansi).
The parser reproduced the saved order, projection, and reference frames.
The field assertions accepted the blank editor and rejected the saved reference.
The recorded F3 bytes matched.
The frame after Esc contained `No result yet.`

The retained host log contained the purchase-order POST trace.
The replay changed no source or captured input and started no services.
It did not run the full live test.
The retained terminal used 50 rows and 260 columns.
F5 after row insertion, the initial empty query, and the current 40-by-160 terminal still need live execution.
Native restart, fresh target state, private logs, and cleanup also need live execution.

The new order label originally used 40 characters.
The table caps a column at 24 characters.
The live helper now uses a 20-character label so that the full label remains visible.
The replay predates this label change.
