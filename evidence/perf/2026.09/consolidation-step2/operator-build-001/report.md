Sixteen generator tests passed at `52ac90a6`.
The [summary](summary.json) records zero failed or ignored tests and unchanged source bytes.
Cargo built all five native debug binaries, including the composed `wamn-receiving` operator.
The [artifact list](artifacts.json) records each path, size, and SHA256 digest.

The [commands](commands.json) use the retained clean environment and two build jobs.
The [archive map](mapping-archives.json) preserves the original source records as gzip files with both digests.
This run did not start the live application environment.
