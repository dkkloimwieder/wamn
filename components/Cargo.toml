[workspace]
resolver = "2"
members = ["api-gateway", "busyloop", "flow-driver", "flowrunner", "hello", "logspewer", "memhog", "node-rs", "pgprobe", "sample-node", "webhook-entry"]

[workspace.package]
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "s"
strip = true
